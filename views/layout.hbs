<!DOCTYPE html>
<html lang="en">
<head>
    {{> head }}
    <title>{{ title }}</title>
</head>
<body>
    {{#if nav}}
        {{> header }}
    {{/if}}

    {{{body}}}

    {{#if footer}}
        {{> footer}}
    {{/if}}
</body>
</html>